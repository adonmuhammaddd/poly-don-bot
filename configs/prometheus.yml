global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'bot'
    static_configs:
      - targets: ['host.docker.internal:9100']
        labels:
          service: 'poly-don-bot'

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
